import 'package:flutter/material.dart';

import 'inicial.dart';

class AppAvaliacao extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Inicial(),
    );
  }
}
